name: Build & Deploy to EC2 via SSM

on:
  push:
    branches:
      - develop  # or main

env:
  IMAGE_NAME: qptrader
  IMAGE_TAG: ${{ github.sha }}
  REGION: ${{ secrets.AWS_REGION }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.REGION }}

<<<<<<< HEAD
      - name: Deploy new containers to EC2 via SSM
=======
      - name: Deploy new containers to EC2 via SSM with Docker Compose
>>>>>>> 315e11ad686aa76830984767b5c8fa713ab625e3
        run: |
          aws ssm send-command \
            --instance-ids "${{ secrets.EC2_INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --comment "Deploying new version with Docker Compose" \
            --parameters 'commands=[
              "mkdir -p ~/qptrader && cd ~/qptrader",
<<<<<<< HEAD
              "echo '\''version: 3.9
=======
              "cat <<EOL > docker-compose.yml
version: '\''3.9'\''
>>>>>>> 315e11ad686aa76830984767b5c8fa713ab625e3
services:
  db:
    image: mysql:8.0
    container_name: qptrader-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: qptrader
      MYSQL_USER: qptraderuser
      MYSQL_PASSWORD: qptraderpass
    ports:
      - '\''3306:3306'\''
    volumes:
      - db_data:/var/lib/mysql
<<<<<<< HEAD
=======

>>>>>>> 315e11ad686aa76830984767b5c8fa713ab625e3
  web:
    image: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
    container_name: qptrader-web
    restart: always
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_USER: qptraderuser
      DB_PASSWORD: qptraderpass
      DB_NAME: qptrader
    ports:
<<<<<<< HEAD
=======
      - '\''8000:8000'\''
volumes:
  db_data:
EOL",
              "sudo docker compose down -v || true",
              "sudo docker compose up -d"
            ]' \
            --region ${{ env.REGION }}
>>>>>>> 315e11ad686aa76830984767b5c8fa713ab625e3
